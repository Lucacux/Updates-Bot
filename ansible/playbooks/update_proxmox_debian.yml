---
- name: Update Proxmox Debian VM
  hosts: proxmox_debian
  become: true
  tasks:
    - name: Actualizar paquetes con apt
      apt:
        update_cache: yes
        upgrade: dist
        autoremove: yes
